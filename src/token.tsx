import {theme} from 'antd';

export const token = {
  token: {
    // Seed Token
    colorPrimary: "#fd9646",
    borderRadius: 0,
    
    // Alias Token
    // colorBgContainer: "black",
  },
  algorithm : theme.defaultAlgorithm
};
